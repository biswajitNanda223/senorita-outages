apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastify-backend.fullname" . }}
  labels:
    {{- include "fastify-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastify-backend.selectorLabels" . | nindent 4 }}
